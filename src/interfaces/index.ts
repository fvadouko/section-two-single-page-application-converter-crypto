export type ICryptoCurrency = {
  id: string;
  name: string;
  symbol: string;
  decimals?: number;
  logoURI?: string;
};

export type ICurrency = {
  id: string;
  name: string;
  symbol: string;
  decimals?: number;
  logoURI?: string;
};
